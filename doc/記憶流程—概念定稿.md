# 記憶流程 — 概念定稿

> 收斂自討論：不維護 MD 對話紀錄、僅以短期 JSON ＋ 長期 JSON 雙池構成記憶；完整對話留給 Telegram 桌面版匯出。對齊 [對話記憶與背版—設計](對話記憶與背版—設計.md)、[對話記憶系統—彙整與探討](對話記憶系統—彙整與探討.md)。

---

## 一、定稿概念摘要

| 項目 | 內容 |
|------|------|
| **對話紀錄（MD）** | **不維護**。使用者若要完整紀錄，使用 Telegram 桌面版「匯出對話」即可；系統不寫、不讀任何對話紀錄檔。 |
| **記憶儲存** | 僅兩類：**短期記憶池（JSON）**、**長期記憶池（JSON）**。 |
| **短期池** | 每輪結束後，用**當下這一輪**（使用者句＋助手句）做 deconstruct，抽出要旨／關鍵事實，直接寫入短期 JSON。 |
| **長期池** | 等同**人物背版**；由短期池在滿足條件時（如 n 輪、時間、或條數上限）經**提煉**後寫入。 |
| **回覆時** | 只讀短期 ＋ 長期池：依本次輸入檢索出幾條，注入 prompt，不讀任何 MD、不傳遞原文對話歷史。 |
| **Context** | 理論上較省：模型從未看到整段對話，只看到固定上限的「精選條目」，token 隨對話長度不膨脹。 |

---

## 二、資料流（概念）

```
使用者發送一句
    ↓
依本次輸入從「短期池」與「長期池」檢索（各取 top-k）
    ↓
背版（SOUL／MEMORY 等）＋ 短期檢索結果 ＋ 長期檢索結果 ＝ 組 prompt
    ↓
模型回覆
    ↓
本輪結束：deconstruct（本輪對話）→ 寫入短期池
    ↓
（背景）若滿足條件：提煉短期池 → 寫入長期池；短期池可清空或滾動
```

**不存在的路徑**：不寫 MD、不讀 MD、不把「最近 N 輪原文」塞進 context。

---

## 三、與既有文件對照

| 既有概念 | 本定稿對應 |
|----------|------------|
| archival、長期記憶 | **長期池 JSON**（人物背版）。 |
| session、短期記憶 | **短期池 JSON**（本場／近期提煉條目）。 |
| 不依賴「MD 對話紀錄 + 硬式檢索」 | 明確不維護 MD；完整紀錄由 Telegram 匯出。 |
| 抽出的要旨 → 少數條目注入 | 短期／長期皆為「條目」；回覆時只注入檢索到的少數幾條。 |
| 背版 blocks（identity／summary／relationship） | 仍可保留 SOUL.md、MEMORY.md 等「固定／可更新背版」；長期池為動態累積的背版補充。 |

---

## 四、設計取捨說明

### 4.1 為何不保留 MD

- Telegram 桌面版已提供匯出對話，人類要看完整紀錄無須系統再存一份。
- 若系統再寫 MD，要嘛（a）從不讀——等於多餘 I/O，要嘛（b）讀來提煉——但短期池已可由「每輪 deconstruct」直接寫入，讀 MD 提煉會多繞一步。故定稿為**不維護 MD**。

### 4.2 短期從「當輪」來、不從 MD 來

- 每輪結束時，系統已有「使用者句＋助手句」在記憶體，直接 deconstruct 寫入短期池即可。
- 不需先寫入 MD、再另時機讀 MD 做提煉；路徑最短、邏輯最單純。

### 4.3 Context 節省

- 回覆路徑只依「本次輸入」從兩池取固定條數（如長期 3 條、短期 2 條），每條可再截斷字數。
- 不塞整段對話、不塞整份 MD，故 token 用量不隨對話長度成長，理論上較省 context。

---

## 五、實作前未定項（不影響概念）

以下留待實作階段再定，不影響「雙池、無 MD、回覆只讀兩池」的概念：

| 項目 | 說明 |
|------|------|
| 短期 → 長期的**觸發條件** | 例如：n 輪、一段時間、短期條數上限、或上下文容量閾值。 |
| **提煉**形式 | 合併相似條目、摘要、或篩選後逐條寫入長期等。 |
| 檢索方式 | 目前為關鍵字重疊；可改為 embedding＋向量檢索，仍維持「只讀兩池、固定條數」。 |
| 條目格式 | 短期／長期 JSON 的欄位（content、tag、timestamp 等）與可選的關鍵字索引。 |

---

## 六、關於模型實際輸出

本設計只決定**送進模型的 context**（背版 ＋ 短期幾條 ＋ 長期幾條），不決定模型「會答出什麼」。實際回答品質仍取決於：

- 模型能力與訓練分佈；
- system prompt 與指令設計；
- deconstruct／提煉的品質（抽得是否精準、是否有噪音）。

因此實作後需依實際對話表現，微調條數、字數上限、以及 deconstruct／提煉的 prompt，必要時可對照 [網上常見的記憶機制—彙整](網上常見的記憶機制—彙整.md) 做小改。

---

## 七、相關文件

- [對話記憶與背版—設計](對話記憶與背版—設計.md)
- [對話記憶與背版—實作步驟與檔案流程](對話記憶與背版—實作步驟與檔案流程.md)
- [對話記憶系統—彙整與探討](對話記憶系統—彙整與探討.md)
- [網上常見的記憶機制—彙整](網上常見的記憶機制—彙整.md)

---

*記憶流程概念定稿 v1*
